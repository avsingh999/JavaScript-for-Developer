var string = '9' /* Stores a bunch of characters */
var number = 9 /* Stores a number */
var array = [1, 2, 3] /* Stores a 'list' of stuff */
var truthy = true /* A true boolean, means 1 in binary */
var falsy = false /* A false boolean, means 0 in binary */
var myObj = {"name": "John", "age": 23} /* Object, stores data about different stuff */
var undefined = undefined /* This means that it isn't defined. */
var null = null /* It basically means nothing, but classified as object */

const name = "Hosé" /* Const means constant, you may not reassign its value */
string = '10' /* I can reassign with the keyword 'var'. You should not put the keyword after assigning */
