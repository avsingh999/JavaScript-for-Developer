let list = [1, 2, 3, 4, 5];

let newList = list.map(element => element*2);

console.log(list); //[ 1, 2, 3, 4, 5 ]

console.log(newList); //[ 2, 4, 6, 8, 10 ]

//Map function use original array to create a new array 
//with it's elements are caculated from elements of original array order by index
 